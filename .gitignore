# Build artifacts
/target/

# Editor/IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# insta pending snapshots — review with `cargo insta review`, never commit.
# CI has a tripwire for these too (.github/workflows/ci.yml).
*.snap.new

# Coverage
/coverage/

# Fuzz artifacts
/fuzz/artifacts/
/fuzz/corpus/

# signoff.md — ephemeral, personal cross-session handoff; never commit.
signoff.md

# issues.md — kaish tracks open work in GitHub Issues and in Amy's local
# exomemory, never in a committed repo file; a second list drifts from both.
/issues.md
